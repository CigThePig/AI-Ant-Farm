# AI Ant Farm

A browser-based ant colony simulation featuring pheromone trails, digging, and a configurable control panel.

## Running locally
Open `index.html` in a modern browser to start the simulation. No build steps or dependencies are required.
