// Shared tile constants used across systems.
// Defined in its own file so modules loaded before script.js can access it safely.
const TILES = {
  GRASS: 0,
  SOIL: 1,
  TUNNEL: 2,
  BEDROCK: 3,
  AIR: 4,
};
